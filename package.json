{
  "name": "green-city-zhijing-webgis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "data:fetch": "node scripts/fetch_overpass.mjs",
    "server:start": "npm --prefix server run start",
    "server:check": "npm --prefix server run check"
  },
  "dependencies": {
    "ol": "^10.6.1",
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.12"
  }
}
